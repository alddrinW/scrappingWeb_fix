# =============================================================================
# CONFIGURACIÓN PRINCIPAL DEL PROYECTO WEB SCRAPING
# =============================================================================
# Copia este archivo a .env y configura los valores según tu entorno

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DEL SERVIDOR
# -----------------------------------------------------------------------------
# IP del servidor donde se ejecuta el proyecto
# Para desarrollo local usa: localhost
# Para servidor remoto usa: tu IP real (ej: 192.168.1.100)
SERVER_IP=localhost

# -----------------------------------------------------------------------------
# PUERTOS DE LOS SERVICIOS
# -----------------------------------------------------------------------------
FRONTEND_PORT=80
BACKEND_PORT=3001
MONGODB_PORT=27018
VNC_PORT=6080

# -----------------------------------------------------------------------------
# URLs PARA EL FRONTEND (se calculan automáticamente)
# -----------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:3001
VITE_VNC_URL=http://localhost:6080

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS
# -----------------------------------------------------------------------------
# Para Docker Compose (usa el nombre del servicio)
MONGODB_URI=mongodb://mongodb:27017
MONGODB_DATABASE=webScraping

# Para desarrollo local del backend (descomenta si desarrollas localmente)
# MONGODB_URI=mongodb://localhost:27017
