# =============================================================================
# CONFIGURACIÓN BACKEND - SOLO PARA DESARROLLO LOCAL
# =============================================================================
# Este archivo se usa ÚNICAMENTE cuando desarrollas el backend por separado
# Para Docker, las variables se toman del .env principal de la raíz

# Configuración de Base de Datos MongoDB para desarrollo local
MONGODB_URI=mongodb://localhost:27017
DB_NAME=webScraping

# Puerto del servidor para desarrollo local
PORT=3001

# Para MongoDB con autenticación (si es necesario):
# MONGODB_URI=mongodb://username:password@localhost:27017/webScraping?authSource=admin

# Para MongoDB Atlas (si usas en la nube):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/webScraping

# NOTA: Para producción con Docker, estas variables se configuran en el .env principal
